import React from 'react';
import { container } from './Modal.module.scss';

interface ModalProps {}

const Modal: React.FC<ModalProps> = () => {
  return (
    <div className={container}>

    </div>
  );
}

export default Modal;
